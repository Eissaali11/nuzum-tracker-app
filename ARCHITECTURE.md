# 🏗️ البنية المعمارية - Architecture

## 📂 هيكل المشروع

```
lib/
├── config/                    # الإعدادات
│   └── api_config.dart        # إعدادات API
│
├── models/                    # نماذج البيانات
│   ├── employee_model.dart    # نموذج الموظف
│   ├── attendance_model.dart  # نموذج الحضور
│   ├── car_model.dart         # نموذج السيارة
│   ├── salary_model.dart      # نموذج الراتب
│   └── operation_model.dart   # نموذج العملية
│
├── services/                  # الخدمات
│   ├── api_service.dart       # خدمة API العامة
│   ├── location_service.dart # خدمة الموقع
│   ├── background_service.dart # خدمة الخلفية
│   └── employee_api_service.dart # خدمة API للموظف
│
├── screens/                   # الشاشات
│   ├── splash_screen.dart      # شاشة البداية
│   ├── setup_screen.dart      # شاشة الإعدادات
│   ├── tracking_screen.dart   # شاشة التتبع
│   ├── employee_profile_screen.dart # صفحة الموظف
│   ├── attendance_list_screen.dart  # قائمة الحضور
│   ├── cars_list_screen.dart        # قائمة السيارات
│   ├── salaries_list_screen.dart   # قائمة الرواتب
│   └── operations_list_screen.dart  # قائمة العمليات
│
├── widgets/                   # الويدجتات المخصصة
│   ├── employee_profile_card.dart   # بطاقة معلومات الموظف
│   ├── employee_photos_card.dart    # بطاقة الصور
│   ├── statistics_card.dart         # بطاقة الإحصائيات
│   ├── attendance_item.dart         # عنصر الحضور
│   ├── car_item.dart                # عنصر السيارة
│   ├── salary_item.dart             # عنصر الراتب
│   └── operation_item.dart          # عنصر العملية
│
├── utils/                     # الأدوات المساعدة
│   └── api_response.dart      # نموذج الاستجابة العام
│
└── main.dart                  # نقطة البداية
```

---

## 🎯 المبادئ المعمارية

### **1. Clean Architecture**
- **Models:** نماذج البيانات المستقلة
- **Services:** طبقة الخدمات (API, Business Logic)
- **Screens:** طبقة العرض (UI)
- **Widgets:** مكونات قابلة لإعادة الاستخدام

### **2. Separation of Concerns**
- كل ملف له مسؤولية واحدة
- فصل منطق الأعمال عن الواجهة
- فصل البيانات عن العرض

### **3. Reusability**
- الويدجتات قابلة لإعادة الاستخدام
- الخدمات قابلة للاستخدام من أي مكان
- النماذج موحدة ومتسقة

---

## 📦 المكونات الرئيسية

### **1. Models (النماذج)**
- **Employee:** بيانات الموظف (الاسم، الرقم الوظيفي، القسم، المنصب، الصور)
- **Attendance:** بيانات الحضور (التاريخ، وقت الدخول/الخروج، الحالة)
- **Car:** بيانات السيارة (رقم اللوحة، الموديل، اللون، الحالة)
- **Salary:** بيانات الراتب (الشهر، المبلغ، تاريخ الدفع، الحالة)
- **Operation:** بيانات العملية (النوع، التاريخ، العميل، العنوان، الحالة)

### **2. Services (الخدمات)**
- **ApiConfig:** إعدادات API (URLs، Timeouts)
- **EmployeeApiService:** خدمة API للموظف (جلب البيانات)
- **LocationApiService:** خدمة الموقع (إرسال الموقع)
- **BackgroundService:** خدمة الخلفية (تتبع الموقع)

### **3. Screens (الشاشات)**
- **EmployeeProfileScreen:** صفحة الموظف الرئيسية
- **AttendanceListScreen:** قائمة الحضور الكاملة
- **CarsListScreen:** قائمة السيارات الكاملة
- **SalariesListScreen:** قائمة الرواتب الكاملة
- **OperationsListScreen:** قائمة العمليات الكاملة

### **4. Widgets (الويدجتات)**
- **EmployeeProfileCard:** بطاقة معلومات الموظف
- **EmployeePhotosCard:** بطاقة الصور (شخصية، هوية، رخصة)
- **StatisticsCard:** بطاقة الإحصائيات
- **AttendanceItem:** عنصر الحضور
- **CarItem:** عنصر السيارة
- **SalaryItem:** عنصر الراتب
- **OperationItem:** عنصر العملية

---

## 🔄 تدفق البيانات

```
User Action
    ↓
Screen (UI)
    ↓
Service (API Call)
    ↓
Model (Data Parsing)
    ↓
Screen (Display)
```

### **مثال:**
1. المستخدم يفتح صفحة الموظف
2. `EmployeeProfileScreen` يستدعي `EmployeeApiService.getEmployeeProfile()`
3. الخدمة ترسل طلب API
4. البيانات تُحول إلى `Employee` model
5. الشاشة تعرض البيانات باستخدام `EmployeeProfileCard`

---

## 🎨 التصميم

### **الألوان:**
- **أساسي:** `#1A237E` (Deep Indigo)
- **ثانوي:** `#0D47A1` (Deep Blue)
- **نجاح:** `#4CAF50` (Green)
- **تحذير:** `#FF9800` (Orange)
- **خطأ:** `#F44336` (Red)

### **المبادئ:**
- ✅ تصميم نظيف بدون أنيميشن
- ✅ متجاوب مع جميع الشاشات
- ✅ دعم اللغة العربية (RTL)
- ✅ ألوان متناسقة ومريحة للعين

---

## 📡 API Integration

### **Base URL:**
```
https://d72f2aef-918c-4148-9723-15870f8c7cf6-00-2c1ygyxvqoldk.riker.replit.dev
```

### **Endpoints:**
- `POST /api/external/employee-profile` - بيانات الموظف
- `POST /api/external/employee-attendance` - الحضور
- `POST /api/external/employee-cars` - السيارات
- `POST /api/external/employee-salaries` - الرواتب
- `POST /api/external/employee-operations` - العمليات

### **Authentication:**
- `api_key`: مفتاح API
- `job_number`: الرقم الوظيفي

---

## ✅ الميزات

### **صفحة الموظف:**
- ✅ عرض معلومات الموظف الشخصية
- ✅ عرض الصور (شخصية، هوية، رخصة)
- ✅ عرض الإحصائيات السريعة
- ✅ عرض آخر 7 أيام حضور
- ✅ عرض السيارات المرتبطة
- ✅ عرض آخر راتب
- ✅ عرض آخر 5 عمليات

### **الصفحات الفرعية:**
- ✅ قائمة الحضور الكاملة
- ✅ قائمة السيارات الكاملة
- ✅ قائمة الرواتب الكاملة
- ✅ قائمة العمليات الكاملة

---

## 🚀 الاستخدام

### **الانتقال إلى صفحة الموظف:**
```dart
Navigator.push(
  context,
  MaterialPageRoute(
    builder: (context) => const EmployeeProfileScreen(),
  ),
);
```

### **جلب بيانات الموظف:**
```dart
final response = await EmployeeApiService.getEmployeeProfile(
  jobNumber: '12345',
  apiKey: 'test_location_key_2025',
);

if (response.success && response.data != null) {
  final employee = response.data!;
  // استخدام البيانات
}
```

---

## 📝 ملاحظات

1. **التصميم:** بدون أنيميشن كما طلبت
2. **الأداء:** استخدام `ListView.builder` للقوائم الطويلة
3. **الأخطاء:** معالجة شاملة للأخطاء مع رسائل واضحة
4. **التحديث:** زر تحديث يدوي في كل صفحة
5. **التنقل:** تنقل سلس بين الصفحات

---

**تاريخ الإنشاء:** 2025-01-27  
**آخر تحديث:** 2025-01-27  
**الحالة:** ✅ مكتمل

