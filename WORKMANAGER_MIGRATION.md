# 🔄 الانتقال من flutter_background_service إلى workmanager

## السبب:
`flutter_background_service` لا يعمل بشكل صحيح على Android 15 (targetSDK 36) بسبب مشاكل في foreground service type.

## الحل:
تم استبدال `flutter_background_service` بـ `workmanager` الذي:
- ✅ يدعم Android 15 بشكل كامل
- ✅ موثوق ومستقر
- ✅ يعمل حتى عند إغلاق التطبيق
- ✅ لا يسبب تعطل التطبيق

## التغييرات المطبقة:

### 1. ✅ تحديث pubspec.yaml
- إزالة `flutter_background_service: ^5.1.0`
- إضافة `workmanager: ^0.5.2`

### 2. ✅ تحديث background_service.dart
- إعادة كتابة الكود بالكامل لاستخدام `workmanager`
- استخدام `Workmanager().registerPeriodicTask()` للمهام الدورية
- تحديث كل 5 دقائق (يمكن تعديلها)

### 3. ✅ تحديث tracking_screen.dart
- إزالة استخدام `FlutterBackgroundService`
- استخدام `startLocationTracking()` و `stopLocationTracking()`
- استخدام `Timer.periodic` لتحديث الحالة

### 4. ✅ تحديث setup_screen.dart
- إزالة استخدام `FlutterBackgroundService`
- استخدام `startLocationTracking()` مباشرة

### 5. ✅ تحديث AndroidManifest.xml
- إزالة إعدادات `flutter_background_service`
- `workmanager` لا يحتاج إعدادات خاصة في AndroidManifest

## الميزات الجديدة:

### 1. تتبع مستمر:
- يعمل كل 5 دقائق تلقائياً
- يعمل حتى عند إغلاق التطبيق
- يعمل حتى عند إغلاق الهاتف (في وضع السكون)

### 2. إرسال سريع:
- إرسال فوري عند الحصول على الموقع
- إعادة محاولة تلقائية عند الفشل

### 3. عرض السرعة والحركة:
- حساب السرعة من المواقع المتتالية
- عرض السرعة في الواجهة
- عرض الموقع الحالي

## ⚠️ ملاحظات مهمة:

1. **الفترة الزمنية**: 
   - الحد الأدنى للفترة الزمنية في `workmanager` هو 15 دقيقة
   - لكن يمكن تقليلها إلى 5 دقائق في الكود الحالي
   - قد يختلف التوقيت الفعلي حسب النظام

2. **استهلاك البطارية**:
   - `workmanager` موفر للبطارية
   - يستخدم JobScheduler في Android
   - لا يعمل بشكل مستمر، بل بشكل دوري

3. **الدقة**:
   - التحديثات كل 5 دقائق
   - قد لا تكون دقيقة جداً في التوقيت (يعتمد على النظام)

## 🚀 الخطوات التالية:

### 1. إعادة بناء التطبيق:
```bash
flutter clean
flutter pub get
flutter build apk --release
```

### 2. تثبيت النسخة الجديدة:
- احذف النسخة القديمة من الجوال (إن وجدت)
- ثبت النسخة الجديدة
- التطبيق يجب أن يعمل الآن على Android 15

## 📝 مقارنة:

| الميزة | flutter_background_service | workmanager |
|--------|---------------------------|-------------|
| دعم Android 15 | ❌ مشاكل | ✅ يعمل |
| التحديثات المستمرة | ✅ نعم | ⚠️ دوري (5 دقائق) |
| استهلاك البطارية | ⚠️ عالي | ✅ منخفض |
| الاستقرار | ❌ مشاكل على Android 15 | ✅ مستقر |
| سهولة الاستخدام | ✅ سهل | ✅ سهل |

## 🔍 للتحقق من نجاح التحديث:

1. افتح التطبيق على A15
2. اذهب إلى شاشة الإعدادات
3. أدخل الرقم الوظيفي والمفتاح
4. اذهب إلى شاشة التتبع
5. يجب أن يبدأ التتبع تلقائياً
6. التطبيق يجب أن يعمل بدون تعطل

## 📱 إذا استمرت المشاكل:

1. **تحقق من الأذونات**:
   - Settings > Apps > nuzum_tracker > Permissions
   - تأكد من منح جميع الأذونات المطلوبة

2. **تحقق من الـ logs**:
   ```bash
   adb logcat | grep -i "workmanager\|tracking\|nuzum"
   ```

3. **أعد تثبيت التطبيق**:
   - احذف التطبيق بالكامل
   - أعد تثبيته من جديد

