# 🔧 إصلاح مشكلة التعطل على Samsung A15

## المشكلة:
التطبيق يتعطل (يخرج) عند التثبيت على جوال Samsung A15

## السبب:
جوال A15 يعمل على Android 14/15، والذي يتطلب أذونات إضافية لم تكن موجودة في التطبيق:
1. **FOREGROUND_SERVICE_LOCATION** - مطلوب لـ Android 12+ (API 31+) عند استخدام foreground service مع الموقع
2. **POST_NOTIFICATIONS** - مطلوب لـ Android 13+ (API 33+) لعرض الإشعارات

بدون هذه الأذونات، النظام يمنع التطبيق من العمل ويسبب التعطل.

## ✅ الحلول المطبقة:

### 1. ✅ إضافة الأذونات المطلوبة
تم إضافة الأذونات التالية إلى `AndroidManifest.xml`:
- `FOREGROUND_SERVICE_LOCATION` - للخدمة الخلفية مع الموقع
- `POST_NOTIFICATIONS` - لعرض الإشعارات

### 2. ✅ إضافة معالجة الأخطاء في main()
تم إضافة try-catch في دالة `main()` لمنع التعطل عند حدوث خطأ أثناء التهيئة:
- إذا حدث خطأ، سيتم عرض رسالة خطأ للمستخدم بدلاً من التعطل
- سيتم طباعة تفاصيل الخطأ في الـ logs للمساعدة في التشخيص

### 3. ✅ إضافة معالجة الأخطاء في MyApplication
تم إضافة try-catch في `MyApplication.onCreate()` لمنع التعطل عند إنشاء قناة الإشعارات:
- التحقق من أن NotificationManager غير null قبل الاستخدام
- تسجيل الأخطاء في الـ logs بدلاً من التعطل

## 🚀 الخطوات المطلوبة الآن:

### 1. إعادة بناء التطبيق:
```bash
flutter clean
flutter pub get
flutter build apk --release
```

### 2. تثبيت النسخة الجديدة:
- احذف النسخة القديمة من الجوال (إن وجدت)
- ثبت النسخة الجديدة
- التطبيق يجب أن يعمل الآن بدون تعطل

## 📝 ملاحظات مهمة:

1. **الأذونات في وقت التشغيل**: 
   - على Android 13+، سيطلب النظام من المستخدم السماح بالإشعارات عند أول تشغيل
   - على Android 12+، سيطلب النظام السماح بخدمة الموقع في الخلفية

2. **التوافق**: 
   - التطبيق الآن متوافق مع Android 12+ (API 31+)
   - الأذونات الجديدة لا تؤثر على الأجهزة القديمة (Android 11 وأقل)

3. **معالجة الأخطاء**:
   - إذا حدث خطأ أثناء التهيئة، سيتم عرض رسالة خطأ بدلاً من التعطل
   - تحقق من الـ logs في Logcat لمعرفة السبب الدقيق لأي مشاكل

## 🔍 للتحقق من نجاح الإصلاح:

1. افتح التطبيق على A15
2. يجب أن يفتح بدون تعطل
3. قد يطلب النظام أذونات (الإشعارات والموقع)
4. بعد منح الأذونات، يجب أن يعمل التطبيق بشكل طبيعي

## 📱 إذا استمر التعطل:

1. **تحقق من الـ logs**:
   ```bash
   adb logcat | grep -i "nuzum\|flutter\|error"
   ```

2. **تحقق من الأذونات**:
   - Settings > Apps > nuzum_tracker > Permissions
   - تأكد من منح جميع الأذونات المطلوبة

3. **أعد تثبيت التطبيق**:
   - احذف التطبيق بالكامل
   - أعد تثبيته من جديد

